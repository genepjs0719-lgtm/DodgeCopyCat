using UnityEngine;

public class GitHubTest : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        깃허브 첨 사용해봐. 연동이 잘 되면 좋겠어
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
